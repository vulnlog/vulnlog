= Vulnlog Suppression

The Vulnlog CLI reads the Vulnlog YAML file and generates suppression files (also called ignore files) for SCA scanners.

Not every reported finding actually affects the project -- false positives and findings already mitigated by the project are common.
Suppression files tell the scanner to stop flagging them.
Vulnlog generates one suppression file per scanner so only the reviewed findings are silenced.

image::vulnlog-suppressing.svg[alt="Vulnlog workflow: SCA scanners report findings, the engineer documents the verdict in vulnlog.yaml, and the Vulnlog CLI generates one ignore file per scanner that feeds back to the scanners"]

== Supported Reporters

[#_cargo_audit]
=== Cargo Audit

Vulnlog supports the https://github.com/RustSec/rustsec/tree/main/cargo-audit[Cargo Audit TOML suppression file] format.

.Cargo Audit toml-based suppression file example.
[source,toml]
----
include::{examplesdir}/suppress/Cargo Audit Suppression Example.toml[]
----

[#_snyk]
=== Snyk

Vulnlog supports the https://docs.snyk.io/manage-risk/policies/the-.snyk-file#use-the-.snyk-file-with-snyk-open-source[Snyk Open Source suppression file] format.

.Snyk yaml-based suppression file example.
[source,yaml]
----
include::{examplesdir}/suppress/Snyk Suppression Example.yaml[lines=1..5]
----

[#_trivy]
=== Trivy

Vulnlog supports the https://trivy.dev/docs/latest/configuration/filtering/#trivyignoreyaml[Trivy YAML suppression file] format.

.Trivy yaml-based suppression file example.
[source,yaml]
----
include::{examplesdir}/suppress/Trivy Suppression Example.yaml[lines=1..4]
----

[#_generic]
=== Generic

Some scanners (such as Semgrep) have no suppression-file mechanism.
Others (such as OWASP Dependency Check) do, but Vulnlog does not yet emit their format.
For both cases Vulnlog writes a generic JSON file that teams can wire into their own tool chain.

You can also request this format for any reporter with `--format generic`, which is useful when you prefer to post-process suppressions in scripts or other tooling rather than use the native format.

.Generic json-based suppression file example.
[source,json]
----
include::{examplesdir}/suppress/Generic Suppression Example.json[]
----

== See also

- xref:suppression.adoc[Suppression Format and Rules]
- xref:cli-suppress.adoc[CLI Suppress Command]
