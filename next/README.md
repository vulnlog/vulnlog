# Vulnlog Next

Vulnlog next is the next iteration of the Vulnlog project. The new version is based on YAML and replaces the previous
Kotlin scripting DSL approach.

The idea is to keep both versions in this repository until the YAML-based new version has feature parity with the Kotlin
DSL.
